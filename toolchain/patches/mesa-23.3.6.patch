diff -rupN old/include/c99_alloca.h new/include/c99_alloca.h
--- old/include/c99_alloca.h
+++ new/include/c99_alloca.h
@@ -41,6 +41,7 @@
 
 #else /* !defined(_MSC_VER) */
 
+#  include <alloca.h>
 #  include <stdlib.h>
 
 #endif /* !defined(_MSC_VER) */
diff -rupN old/include/drm-uapi/drm.h new/include/drm-uapi/drm.h
--- old/include/drm-uapi/drm.h
+++ new/include/drm-uapi/drm.h
@@ -41,6 +41,12 @@
 #include <asm/ioctl.h>
 typedef unsigned int drm_handle_t;
 
+#elif defined(__lyos__)
+
+#include <lyos/types.h>
+#include <asm/ioctl.h>
+typedef unsigned int drm_handle_t;
+
 #else /* One of the BSDs */
 
 #include <stdint.h>
diff -rupN old/meson.build new/meson.build
--- old/meson.build
+++ new/meson.build
@@ -121,7 +121,7 @@
 # Only build shared_glapi if at least one OpenGL API is enabled
 with_shared_glapi = with_shared_glapi and with_any_opengl
 
-system_has_kms_drm = ['openbsd', 'netbsd', 'freebsd', 'gnu/kfreebsd', 'dragonfly', 'linux', 'sunos', 'android'].contains(host_machine.system())
+system_has_kms_drm = ['openbsd', 'netbsd', 'freebsd', 'gnu/kfreebsd', 'dragonfly', 'linux', 'sunos', 'android', 'lyos'].contains(host_machine.system())
 
 gallium_drivers = get_option('gallium-drivers')
 if gallium_drivers.contains('auto')
@@ -959,7 +959,7 @@
 endif
 
 # TODO: this is very incomplete
-if ['linux', 'cygwin', 'gnu', 'freebsd', 'gnu/kfreebsd', 'haiku', 'android'].contains(host_machine.system())
+if ['linux', 'cygwin', 'gnu', 'freebsd', 'gnu/kfreebsd', 'haiku', 'android', 'lyos'].contains(host_machine.system())
   pre_args += '-D_GNU_SOURCE'
 elif host_machine.system() == 'sunos'
   pre_args += '-D__EXTENSIONS__'
diff -rupN old/src/compiler/glsl/glsl_lexer.ll new/src/compiler/glsl/glsl_lexer.ll
--- old/src/compiler/glsl/glsl_lexer.ll
+++ new/src/compiler/glsl/glsl_lexer.ll
@@ -22,7 +22,7 @@
  * DEALINGS IN THE SOFTWARE.
  */
 #include <ctype.h>
-#include <limits.h>
+#include <climits>
 #include "util/strtod.h"
 #include "ast.h"
 #include "glsl_parser_extras.h"
diff -rupN old/src/util/detect_os.h new/src/util/detect_os.h
--- old/src/util/detect_os.h
+++ new/src/util/detect_os.h
@@ -81,6 +81,11 @@
 #define DETECT_OS_UNIX 1
 #endif
 
+#if defined(__lyos__)
+#define DETECT_OS_LYOS 1
+#define DETECT_OS_UNIX 1
+#endif
+
 
 /*
  * Make sure DETECT_OS_* are always defined, so that they can be used with #if
@@ -112,6 +117,9 @@
 #ifndef DETECT_OS_LINUX
 #define DETECT_OS_LINUX 0
 #endif
+#ifndef DETECT_OS_LYOS
+#define DETECT_OS_LYOS 0
+#endif
 #ifndef DETECT_OS_NETBSD
 #define DETECT_OS_NETBSD 0
 #endif
diff -rupN old/src/util/os_file.c new/src/util/os_file.c
--- old/src/util/os_file.c
+++ new/src/util/os_file.c
@@ -35,7 +35,11 @@
    int fd = open(filename, O_CREAT | O_EXCL | O_WRONLY, filemode);
    if (fd == -1)
       return NULL;
+#if DETECT_OS_LYOS
+   return _fdopen_r(_REENT, fd, "w");
+#else
    return fdopen(fd, "w");
+#endif
 }
 
 
diff -rupN old/src/util/os_misc.c new/src/util/os_misc.c
--- old/src/util/os_misc.c
+++ new/src/util/os_misc.c
@@ -57,7 +57,7 @@
 #  include <unistd.h>
 #  include <log/log.h>
 #  include <cutils/properties.h>
-#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD
+#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_LYOS
 #  include <unistd.h>
 #elif DETECT_OS_OPENBSD || DETECT_OS_FREEBSD
 #  include <sys/resource.h>
@@ -248,7 +248,7 @@
 bool
 os_get_total_physical_memory(uint64_t *size)
 {
-#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD
+#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_LYOS
    const long phys_pages = sysconf(_SC_PHYS_PAGES);
    const long page_size = sysconf(_SC_PAGE_SIZE);
 
diff -rupN old/src/util/u_endian.h new/src/util/u_endian.h
--- old/src/util/u_endian.h
+++ new/src/util/u_endian.h
@@ -82,7 +82,7 @@
 # define UTIL_ARCH_BIG_ENDIAN 1
 #endif
 
-#elif defined(_WIN32) || defined(ANDROID)
+#elif defined(_WIN32) || defined(ANDROID) || defined(__lyos__)
 
 #define UTIL_ARCH_LITTLE_ENDIAN 1
 #define UTIL_ARCH_BIG_ENDIAN 0
